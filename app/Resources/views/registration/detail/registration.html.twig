{% extends 'base.html.twig' %}

{% block body %}
    <div id="smashtube-content">
        <h1></h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}

        {{  form_errors(form) }}
    </div>
{% endblock %}